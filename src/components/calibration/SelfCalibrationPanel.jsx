import React from'react';import{Panel}from'@/components/ui/Trading';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import useCalibrationManager from'@/hooks/useCalibrationManager';import{OBJECTIVE_MODES}from'@/lib/selfCalibration';import CalibrationActions from'./CalibrationActions';import CalibrationSummary from'./CalibrationSummary';import CalibrationProfiles from'./CalibrationProfiles';import CalibrationDiagnostics from'./CalibrationDiagnostics';import CalibrationImpactPreview from'./CalibrationImpactPreview';
export default function SelfCalibrationPanel(){const manager=useCalibrationManager();return <Panel className="p-4 space-y-4"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="font-heading font-semibold">Self-calibration</h2><p className="text-xs text-muted-foreground">Paper-only, out-of-sample optimisation. No profit is promised and champions never auto-apply.</p></div><Select value={manager.objectiveMode} onValueChange={manager.setObjectiveMode}><SelectTrigger className="w-64"><SelectValue/></SelectTrigger><SelectContent>{OBJECTIVE_MODES.map(mode=><SelectItem key={mode} value={mode}>{mode.replaceAll('_',' ')}</SelectItem>)}</SelectContent></Select></div><CalibrationActions manager={manager}/><CalibrationSummary result={manager.result} champion={manager.currentChampion}/><CalibrationProfiles profiles={manager.profiles}/><CalibrationImpactPreview manager={manager}/><CalibrationDiagnostics manager={manager}/><div className="text-xs text-muted-foreground">No-trade baseline enabled · PLACE and H2H remain disabled · hard safety and risk limits cannot be relaxed automatically.</div></Panel>}