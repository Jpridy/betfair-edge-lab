import React from'react';
import{useApp}from'@/lib/AppContext';
import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'@/components/ui/table';
export default function EffectiveSettingsTable(){const{effectiveSettings}=useApp();return <div className="overflow-auto rounded-lg border border-border"><Table><TableHeader><TableRow><TableHead>Setting</TableHead><TableHead>Stored</TableHead><TableHead>Effective</TableHead><TableHead>Unit</TableHead><TableHead>Source</TableHead><TableHead>Engine consumers</TableHead><TableHead>Linked</TableHead></TableRow></TableHeader><TableBody>{effectiveSettings.linkage.map(item=><TableRow key={`${item.source}.${item.settingKey}`}><TableCell>{item.displayName}</TableCell><TableCell className="font-mono text-xs">{JSON.stringify(item.storedValue)}</TableCell><TableCell className="font-mono text-xs">{JSON.stringify(item.effectiveValue)}</TableCell><TableCell>{item.unit}</TableCell><TableCell>{item.source}</TableCell><TableCell className="text-xs">{item.engineConsumers.join(', ')}</TableCell><TableCell>{item.linked&&!item.validationError?'Yes':'No'}</TableCell></TableRow>)}</TableBody></Table></div>}