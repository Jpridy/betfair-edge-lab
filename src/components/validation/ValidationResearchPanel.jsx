import React from'react';
import{useApp}from'@/lib/AppContext';
const pct=value=>value==null?'—':`${(value*100).toFixed(2)}%`;
export default function ValidationResearchPanel(){const{statisticalValidation:v}=useApp();const rows=[['Train sample',v.trainBets],['Validation sample',v.validationBets],['Test sample',v.testBets],['Train net ROI',pct(v.trainROI)],['Validation net ROI',pct(v.validationROI)],['Test net ROI',pct(v.testROI)],['Bootstrap 95% lower ROI',pct(v.bootstrapLower95ROI)],['Brier score',v.brierScore==null?'—':v.brierScore.toFixed(4)],['Test max drawdown',`$${v.maxDrawdown.toFixed(2)}`]];return <section className="rounded-lg border border-border bg-card p-4"><h2 className="text-sm font-semibold">Out-of-sample validation</h2><div className="mt-3 grid grid-cols-2 gap-3 md:grid-cols-3">{rows.map(([label,value])=><div key={label}><div className="text-[10px] uppercase tracking-label text-muted-foreground">{label}</div><div className="mt-1 font-mono text-sm">{value}</div></div>)}</div></section>}