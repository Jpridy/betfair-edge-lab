import{describe,expect,it}from'vitest';import{SETTINGS_REGISTRY,resolveEffectiveSettings}from'./settingsRegistry';import{PAPER_VALIDATION_PRESET}from'./paperValidationPreset';
describe('effective settings registry',()=>{it('registers and links every preset setting',()=>{const expected=Object.entries(PAPER_VALIDATION_PRESET).filter(([k])=>k!=='validationRules').reduce((n,[,v])=>n+Object.keys(v).length,0);expect(SETTINGS_REGISTRY).toHaveLength(expected);expect(SETTINGS_REGISTRY.every(x=>x.engineConsumers.length>0)).toBe(true);});it('enforces immutable paper-only safety',()=>{const r=resolveEffectiveSettings({appSettings:{liveTradingEnabled:true,forcedPaperOnlyMode:false},botSettings:{liveTradingEnabled:true,botMode:'live'},featherlessSettings:{allowLiveHandoff:true,allowDeterministicFallback:true}});expect(r.appSettings).toMatchObject({forcedPaperOnlyMode:true,liveTradingEnabled:false,allowInPlay:false});expect(r.botSettings).toMatchObject({botMode:'paper',liveTradingLocked:true,liveTradingEnabled:false});expect(r.featherlessSettings).toMatchObject({allowLiveHandoff:false,allowDeterministicFallback:false,placeOrdersEnabled:false,h2hOrdersEnabled:false});expect(Object.isFrozen(r.appSettings)).toBe(true);});});